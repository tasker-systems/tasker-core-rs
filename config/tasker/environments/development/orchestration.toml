# Development Environment - Orchestration Configuration Overrides

[orchestration]
enable_performance_logging = true

# TAS-49: DLQ Configuration Overrides for Development Environment
[orchestration.dlq.staleness_detection]
detection_interval_seconds = 120  # More frequent detection for dev (2 minutes)
batch_size = 50
dry_run = false

[orchestration.dlq.staleness_detection.thresholds]
waiting_for_dependencies_minutes = 15  # Shorter for dev iteration
waiting_for_retry_minutes = 10
steps_in_process_minutes = 15
task_max_lifetime_hours = 4

# TAS-59: Batch Processing Configuration Overrides for Development Environment
# TAS-125: Removed checkpoint_interval_default - handlers decide when to checkpoint
[orchestration.batch_processing]
enabled = true                   # Enable batch processing for development
max_parallel_batches = 25        # Moderate limit for dev
default_batch_size = 500         # Moderate batch size
checkpoint_stall_minutes = 5     # Faster stall detection for dev iteration
