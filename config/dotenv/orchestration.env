# Orchestration-specific environment variables
# Appended to base.env + test.env for orchestration service

# Service identification
PORT=8080
TASKER_WEB_BIND_ADDRESS=0.0.0.0:8080

# TAS-177: gRPC API endpoint
TASKER_ORCHESTRATION_GRPC_BIND_ADDRESS=0.0.0.0:9090

# Orchestration configuration
TASKER_CONFIG_PATH=${WORKSPACE_PATH}/config/tasker/generated/complete-test.toml
