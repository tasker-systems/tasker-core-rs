# Split database overrides - TAS-78
# Appended to test.env when using split-database mode
#
# This configures separate databases for Tasker and PGMQ on port 5433
# Requires: docker compose -f docker/docker-compose.dual-pg.test.yml up -d

# Override database URLs for split mode (port 5433)
DATABASE_URL=postgresql://tasker:tasker@localhost:5433/tasker_split_test
TEST_DATABASE_URL=postgresql://tasker:tasker@localhost:5433/tasker_split_test

# PGMQ on separate database
PGMQ_DATABASE_URL=postgresql://tasker:tasker@localhost:5433/pgmq_split_test

# SQLx offline mode required for split-db builds (compile-time verification uses single DB)
SQLX_OFFLINE=true
