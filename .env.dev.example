# =============================================================================
# Development Environment Configuration
# =============================================================================
# Copy this file to .env.dev and source it before running Tasker services
# Usage: source .env.dev && cargo run --bin tasker-server
#
# TAS-29 Phase 3: OpenTelemetry integration with local Grafana LGTM stack

# =============================================================================
# Database Configuration
# =============================================================================
DATABASE_URL=postgresql://tasker:tasker@localhost:5432/tasker_rust_development

# =============================================================================
# OpenTelemetry Configuration (TAS-29 Phase 3)
# =============================================================================
# Enable distributed tracing
TELEMETRY_ENABLED=true

# Service identification
OTEL_SERVICE_NAME=tasker-orchestration
OTEL_SERVICE_VERSION=0.1.0

# OTLP endpoint (Grafana LGTM stack from docker-compose.dev.yml)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# Sampling rate (1.0 = 100% of traces, 0.1 = 10% of traces)
OTEL_TRACES_SAMPLER_ARG=1.0

# Deployment environment
DEPLOYMENT_ENVIRONMENT=development

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level: trace, debug, info, warn, error
RUST_LOG=info

# Show backtraces on panic
RUST_BACKTRACE=1

# =============================================================================
# Tasker Configuration
# =============================================================================
TASKER_ENV=development

# =============================================================================
# Optional: Additional Configuration
# =============================================================================
# Uncomment to customize worker behavior
# WORKER_ID=dev-worker-001
# TASKER_TEMPLATE_PATH=/path/to/custom/templates

# Uncomment for verbose OpenTelemetry debugging
# RUST_LOG=debug,opentelemetry=trace,tracing_opentelemetry=trace
