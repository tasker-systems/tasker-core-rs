# TaskTemplate Configuration - Python Implementation
#
# Simple Success Handler for E2E Testing
# Demonstrates a minimal successful handler execution via Python FFI
#
# Template: test_scenarios/success_only_py:1.0.0
# Implementation: Python FFI with tasker-worker-py (PyO3)
#
---
name: success_only_py
namespace_name: test_scenarios
version: 1.0.0
description: "Simple single-step handler that always succeeds (Python)"
metadata:
  author: Python FFI Implementation
  tags:
    - namespace:test_scenarios
    - pattern:single_step
    - implementation:python_ffi
    - language:python
    - test_type:success
  created_at: "2025-12-13T00:00:00Z"
  updated_at: "2025-12-13T00:00:00Z"
task_handler:
  callable: test_scenarios.SuccessOnlyHandler
  initialization: {}
system_dependencies:
  primary: default
  secondary: []
domain_events: []
input_schema:
  type: object
  properties:
    message:
      type: string
      description: "Optional message to include in result"
steps:
  - name: success_step_py
    description: "A step that always succeeds"
    handler:
      callable: test_scenarios.step_handlers.SuccessStepHandler
      initialization:
        expected_outcome: success
    system_dependency:
    dependencies: []
    retry:
      retryable: true
      max_attempts: 3
      backoff: exponential
      backoff_base_ms: 1000
      max_backoff_ms: 30000
    timeout_seconds: 30
    publishes_events: []
environments:
  test:
    steps:
      - name: success_step_py
        timeout_seconds: 10
        retry:
          max_attempts: 2
