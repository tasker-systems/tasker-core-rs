# TaskTemplate Configuration - TypeScript Implementation
#
# Simple Success-Only Workflow for Basic Testing
# Single step that always succeeds - used for basic integration testing
#
# Template: test_scenarios/success_only_ts:1.0.0
# Implementation: TypeScript FFI with tasker-worker-ts
#
---
name: success_only_ts
namespace_name: test_scenarios_ts
version: 1.0.0
description: "Simple success-only workflow for basic TypeScript integration testing"
metadata:
  author: TypeScript FFI Implementation
  tags:
    - namespace:test_scenarios_ts
    - pattern:single_step
    - implementation:typescript_ffi
    - language:typescript
    - type_safety:compile_time
  documentation_url:
  created_at: "2025-12-23T00:00:00Z"
  updated_at: "2025-12-23T00:00:00Z"
  notes: "TypeScript FFI implementation for basic success testing"
task_handler:
  callable: TestScenarios.SuccessOnlyHandler
  initialization:
    expected_outcome: success
system_dependencies:
  primary: default
  secondary: []
domain_events: []
input_schema:
  type: object
  required: []
  properties:
    message:
      type: string
      description: "Optional message to echo in the result"
steps:
  - name: success_step
    description: "Simple step that always succeeds"
    handler:
      callable: TestScenarios.StepHandlers.SuccessStepHandler
      initialization:
        operation: echo
        expected_outcome: success
    system_dependency:
    dependencies: []
    retry:
      retryable: true
      max_attempts: 3
      backoff: exponential
      backoff_base_ms: 1000
      max_backoff_ms: 30000
    timeout_seconds: 30
    publishes_events: []
environments:
  test:
    steps:
      - name: success_step
        timeout_seconds: 10
        retry:
          max_attempts: 2
