---
name: boundary_max_attempts_one
namespace_name: test_sql
version: 1.0.0
description: "Boundary test: max_attempts=1 means exactly one total attempt"
metadata:
  author: "Test Infrastructure"
  tags:
    - namespace:test_sql
    - pattern:boundary_testing
task_handler:
  callable: TestHandlers::BoundaryTestHandler
system_dependencies:
  primary: test_system
  secondary: []
steps:
  - name: boundary_test_step
    description: "Test step with max_attempts=1"
    handler:
      callable: TestHandlers::NoOpHandler
    dependencies: []
    retry:
      retryable: true
      max_attempts: 1
      backoff: exponential
      backoff_base_ms: 100
    timeout_seconds: 5
