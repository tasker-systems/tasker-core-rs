---
name: boundary_max_attempts_zero
namespace_name: test_sql
version: 1.0.0
description: "Boundary test: max_attempts=0 should allow first execution"
metadata:
  author: "Test Infrastructure"
  tags:
    - namespace:test_sql
    - pattern:boundary_testing
task_handler:
  callable: TestHandlers::BoundaryTestHandler
system_dependencies:
  primary: test_system
  secondary: []
steps:
  - name: boundary_test_step
    description: "Test step with max_attempts=0"
    handler:
      callable: TestHandlers::NoOpHandler
    dependencies: []
    retry:
      retryable: true
      max_attempts: 0
      backoff: exponential
      backoff_base_ms: 100
    timeout_seconds: 5
