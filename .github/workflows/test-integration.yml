name: Integration Tests

on:
  workflow_call:
    inputs:
      postgres-image:
        required: true
        type: string

jobs:
  # ============================================================================
  # Stage 1: Unit Tests
  # Run library tests and doctests for core packages
  # Uses cache warmed by build-workers workflow
  # ============================================================================
  unit-tests:
    name: Unit Tests + Doctests
    runs-on: ubuntu-22.04
    timeout-minutes: 15

    services:
      postgres:
        image: ${{ inputs.postgres-image }}
        ports:
          - "5432:5432"
        env:
          POSTGRES_USER: tasker
          POSTGRES_PASSWORD: tasker
          POSTGRES_DB: tasker_rust_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Rust build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-integration-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-integration-
            ${{ runner.os }}-cargo-

      - name: Setup shared environment variables
        uses: ./.github/actions/setup-env

      - name: Install tools
        uses: ./.github/actions/install-tools
        with:
          tools: "nextest sqlx-cli make"

      - name: Setup database
        run: sqlx migrate run
        env:
          DATABASE_URL: postgresql://tasker:tasker@localhost:5432/tasker_rust_test

      - name: Run unit tests
        run: |
          echo "🧪 Running unit tests..."
          mkdir -p target/nextest/ci
          cargo nextest run \
            --profile ci \
            --lib \
            --package tasker-shared \
            --package tasker-orchestration \
            --package tasker-worker \
            --package pgmq-notify \
            --package tasker-client \
            --no-fail-fast
        env:
          DATABASE_URL: postgresql://tasker:tasker@localhost:5432/tasker_rust_test

      - name: Run doctests
        run: |
          echo "📚 Running documentation tests..."
          cargo test --doc \
            --package tasker-shared \
            --package tasker-orchestration \
            --package tasker-worker \
            --package pgmq-notify \
            --package tasker-client
        env:
          DATABASE_URL: postgresql://tasker:tasker@localhost:5432/tasker_rust_test

      - name: Upload unit test results
        uses: actions/upload-artifact@v6
        if: always() && hashFiles('target/nextest/ci/junit.xml') != ''
        with:
          name: unit-test-results
          path: target/nextest/ci/junit.xml

  # ============================================================================
  # Stage 2: Integration Tests (E2E)
  # Run E2E tests with all services running
  # Uses artifacts from build-workers workflow
  # ============================================================================
  integration-tests:
    name: Integration Tests (E2E)
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    needs: unit-tests

    services:
      postgres:
        image: ${{ inputs.postgres-image }}
        ports:
          - "5432:5432"
        env:
          POSTGRES_USER: tasker
          POSTGRES_PASSWORD: tasker
          POSTGRES_DB: tasker_rust_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Free up disk space
        uses: ./.github/actions/free-disk-space

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          bundler-cache: true
          working-directory: workers/ruby

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Restore Rust build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-integration-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-integration-
            ${{ runner.os }}-cargo-

      - name: Setup shared environment variables
        uses: ./.github/actions/setup-env

      - name: Install tools
        uses: ./.github/actions/install-tools
        with:
          tools: "nextest sqlx-cli make"

      - name: Download build artifacts
        uses: actions/download-artifact@v7
        with:
          name: build-artifacts
          path: artifacts/build

      - name: Download worker artifacts
        uses: actions/download-artifact@v7
        with:
          name: worker-artifacts
          path: artifacts/workers
        continue-on-error: true

      - name: Restore artifacts to correct locations
        run: |
          echo "📦 Restoring build artifacts..."

          # Restore core binaries
          mkdir -p target/debug
          if [ -d artifacts/build ]; then
            cp -f artifacts/build/tasker-* target/debug/ 2>/dev/null || true
            chmod +x target/debug/tasker-* 2>/dev/null || true
          fi
          echo "✅ Core binaries restored"
          ls -lh target/debug/tasker-server target/debug/tasker-worker target/debug/tasker-cli 2>/dev/null || echo "⚠️  Some binaries missing"

          # Restore Ruby extension from worker-artifacts
          if [ -d artifacts/workers ]; then
            mkdir -p workers/ruby/lib/tasker_core
            # Handle nested path structure
            find artifacts/workers -name "*.so" -exec cp {} workers/ruby/lib/tasker_core/ \; 2>/dev/null || true
            find artifacts/workers -name "*.bundle" -exec cp {} workers/ruby/lib/tasker_core/ \; 2>/dev/null || true
            # Restore rust-worker binary
            find artifacts/workers -name "rust-worker" -exec cp {} target/debug/ \; 2>/dev/null || true
            chmod +x target/debug/rust-worker 2>/dev/null || true
            echo "✅ Worker artifacts restored"
          fi

      - name: Build Ruby FFI extension (if not from artifacts)
        run: |
          cd workers/ruby
          if ls lib/tasker_core/*.so 2>/dev/null || ls lib/tasker_core/*.bundle 2>/dev/null; then
            echo "✅ Ruby extension already available from artifacts"
            ls -lh lib/tasker_core/
          else
            echo "🔨 Building Ruby extension from source..."
            bundle install
            bundle exec rake compile
          fi
        env:
          SQLX_OFFLINE: "true"

      - name: Build Python FFI extension
        run: |
          echo "🔨 Building Python FFI extension (uses warm cache)..."
          cd workers/python
          uv sync --dev
          uv run maturin develop
        env:
          SQLX_OFFLINE: "true"

      - name: Build Rust worker (if not from artifacts)
        run: |
          if [ -f target/debug/rust-worker ]; then
            echo "✅ Rust worker already available from artifacts"
          else
            echo "🔨 Building Rust worker from source..."
            cargo build --package tasker-worker-rust
          fi
        env:
          SQLX_OFFLINE: "true"

      - name: Setup TypeScript worker
        run: |
          echo "📜 Setting up TypeScript worker..."
          cd workers/typescript
          bun install --frozen-lockfile

          # Build the TypeScript worker (compiles to dist/)
          bun run build

          # The FFI library should be available from the release build
          # For CI, we need to ensure the path is correct
          if [ -f "../../target/release/libtasker_worker.so" ]; then
            echo "✅ FFI library found at target/release/libtasker_worker.so"
          else
            echo "⚠️ FFI library not found, building..."
            cargo build --release -p tasker-worker-ts
          fi
        env:
          DATABASE_URL: postgresql://tasker:tasker@localhost:5432/tasker_rust_test

      - name: Run database migrations
        run: sqlx migrate run
        env:
          DATABASE_URL: postgresql://tasker:tasker@localhost:5432/tasker_rust_test

      - name: Generate test configuration
        run: |
          mkdir -p config/tasker
          echo "🔧 Generating orchestration configuration..."
          cargo run --package tasker-client --bin tasker-cli -- config generate \
            --context orchestration \
            --environment test \
            --source-dir config/tasker \
            --output config/tasker/orchestration-test.toml
          echo "🔧 Generating worker configuration..."
          cargo run --package tasker-client --bin tasker-cli -- config generate \
            --context worker \
            --environment test \
            --source-dir config/tasker \
            --output config/tasker/worker-test.toml
        env:
          DATABASE_URL: postgresql://tasker:tasker@localhost:5432/tasker_rust_test

      - name: Start native services
        run: |
          mkdir -p .pids
          chmod +x .github/scripts/start-native-services.sh
          .github/scripts/start-native-services.sh
        env:
          DATABASE_URL: postgresql://tasker:tasker@localhost:5432/tasker_rust_test

      - name: Run integration tests
        run: |
          echo "🏁 Running integration tests..."
          mkdir -p target/nextest/ci
          cargo nextest run \
            --profile ci \
            --test '*' \
            --no-fail-fast
        env:
          DATABASE_URL: postgresql://tasker:tasker@localhost:5432/tasker_rust_test
          TASKER_TEST_ORCHESTRATION_URL: http://localhost:8080
          TASKER_TEST_WORKER_URL: http://localhost:8081
          TASKER_TEST_RUBY_WORKER_URL: http://localhost:8082
          TASKER_TEST_PYTHON_WORKER_URL: http://localhost:8083
          TASKER_TEST_TYPESCRIPT_WORKER_URL: http://localhost:8084
          TASKER_FIXTURE_PATH: ${{ github.workspace }}/tests/fixtures
          RUST_LOG: info

      - name: Upload integration test results
        uses: actions/upload-artifact@v6
        if: always() && hashFiles('target/nextest/ci/junit.xml') != ''
        with:
          name: integration-test-results
          path: target/nextest/ci/junit.xml

      - name: Collect service logs on failure
        if: failure()
        run: |
          mkdir -p service-logs
          [ -f orchestration.log ] && cp orchestration.log service-logs/
          [ -f worker.log ] && cp worker.log service-logs/
          [ -f ruby-worker.log ] && cp ruby-worker.log service-logs/
          [ -f python-worker.log ] && cp python-worker.log service-logs/

      - name: Upload service logs
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: service-logs
          path: service-logs/

      - name: Stop native services
        if: always()
        run: |
          chmod +x .github/scripts/stop-native-services.sh
          .github/scripts/stop-native-services.sh
